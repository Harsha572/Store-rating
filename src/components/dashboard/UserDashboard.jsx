import React from 'react';

const UserDashboard = () => {
  return (
    <div className="dashboard-container">
      <h2>Welcome to the User Dashboard</h2>
      <p>You can view and rate stores from here.</p>
      {/* Add store list, rating forms, etc. here */}
    </div>
  );
};

export default UserDashboard;
